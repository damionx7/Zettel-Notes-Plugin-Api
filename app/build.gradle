plugins {
    id 'com.android.library'
    id 'maven-publish'
}

def appVersion = 11
def buttonApiVersion = 9
def scanApiVersion = 1

android {
    namespace "org.eu.thedoc.zettelnotes.interfaces"
    compileSdk 33

    defaultConfig {
        minSdk 21
        targetSdk 33
        versionCode appVersion
        versionName "${appVersion}"
        //button interface
        resValue "string", "api_version", "\"${buttonApiVersion}\""
        buildConfigField("String", "API_VERSION", "\"${buttonApiVersion}\"")
        //scan interface
        resValue "string", "scan_api_version", "\"${scanApiVersion}\""
        buildConfigField("String", "SCAN_API_VERSION", "\"${scanApiVersion}\"")

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        debug {
            debuggable true
            minifyEnabled false
        }
        release {
            //disable proguard for plugin system to work
            minifyEnabled false
            debuggable false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            consumerProguardFiles "consumer-rules.pro"
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    publishing {
        multipleVariants {
            includeBuildTypeValues('release')
        }
    }

}

dependencies {
    api "androidx.appcompat:appcompat:1.6.1"
}

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId = 'com.github.damionx7'
                artifactId = 'Zettel-Notes-Plugin-Api'
                version = "${appVersion}"

                afterEvaluate {
                    from components.default
                }
            }
        }
    }
}
